# PyBodyTrack
 Small interactive minigames and keyboard with python tools for real time tracking. A script that stores the information into an csv and another script that reconstructs it in an 3D array, where the landmarks form a stick figure skeleton animating it
